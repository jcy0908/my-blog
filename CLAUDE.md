# my-blog

마크다운 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트.

## 기술 스택

- **프레임워크 없음** — 순수 HTML, CSS, JavaScript만 사용 (React, Vue, Next.js 등 금지)
- 빌드 도구나 번들러 없이 브라우저에서 바로 동작하는 것을 지향 (필요 시 최소한의 빌드 스크립트만 허용)
- 외부 런타임 의존성(npm 패키지 등) 최소화. CDN 라이브러리 추가 전에는 정말 필요한지 검토할 것

## 핵심 기능

- `posts/` (또는 유사 디렉토리)에 있는 마크다운 파일을 읽어 HTML로 파싱
- 마크다운 → HTML 변환은 직접 구현하거나 가벼운 파서 사용 (프레임워크 의존 없이)
- 글 목록 페이지 + 개별 포스트 페이지 라우팅

## 디자인 원칙

- **깔끔하고 읽기 좋은 타이포그래피** 우선 — 본문 가독성이 최우선 (적절한 줄 간격, 본문 폭 제한 등)
- **다크 모드 지원** — `prefers-color-scheme` 기반 자동 감지 + 수동 토글, CSS 변수로 색상 관리
- **모바일 반응형** — 모바일 퍼스트로 작성, 작은 화면에서도 레이아웃/타이포그래피 깨지지 않도록 확인
- 불필요한 장식이나 애니메이션 지양, 콘텐츠 중심 UI

## 코드 컨벤션

- CSS는 커스텀 프로퍼티(CSS variables)로 라이트/다크 테마 색상을 관리
- JavaScript는 바닐라 ES 모듈 사용, 불필요한 추상화 지양
- 파일/폴더 구조가 커지기 전까지는 과도하게 분리하지 말 것
