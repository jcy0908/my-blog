# style.md — 색과 토큰의 단일 출처

이 문서는 `CLAUDE.md`의 웹앱 규칙이 참조하는 파일입니다. 새 웹앱을 만들 때 색과 움직임을 새로 고르지 말고 여기서 가져옵니다.

값의 출처는 `public/styles.css`이고, 모션·재료·타이포의 근거는 `plugins/apple-design/skills/apple-design/SKILL.md`입니다. 이 문서와 그 파일들이 어긋나면 그쪽이 맞습니다 — 발견하면 이 문서를 고쳐 주세요.

## 원칙 — 색은 한 개뿐이다

바탕은 강원 영서(嶺西)의 겨울에서 채집한 무채색이고, **색을 가진 것은 이끼색(`--moss`) 하나뿐입니다.**

비비드한 색은 화폐 같아서 많이 찍어 내면 가치가 떨어집니다. 화면 전체가 색이면 아무것도 강조되지 않습니다. 그래서 무채색으로 바탕의 값을 낮춰 두고 딱 한 곳에만 색을 지불합니다.

실무적으로는 이렇게 됩니다. **색이 나타나는 자리가 곧 눌러야 할 자리입니다.** 링크, 강조, 라벨, 활성 상태에만 이끼색을 쓰고, 그 외에는 쓰지 않습니다. 장식으로 쓰는 순간 이 규칙이 무너집니다.

같은 절제가 움직임에도 적용됩니다. **모션은 반응이지 장식이 아닙니다.** 사용자가 건드린 것만 움직입니다. 저절로 움직이는 것, 시선을 끌려고 흔들리는 것은 넣지 않습니다.

## 1. 바탕색 여섯 — 영서의 겨울

```css
--winter-haze:  #eef1f2;   /* 겨울 안개 — 가장 밝은 바탕 */
--ice-sheet:    #dbe2e4;   /* 성엣장 — 한 단 낮은 면 */
--mountain-ink: #1c2325;   /* 먹색 산 — 본문 글자 */
--moss:         #4b6560;   /* 이끼 — 유일한 색 */
--stone:        #8a9296;   /* 돌 — 보조 글자 */
--winter-soil:  #5d6a6d;   /* 겨울 흙 */
```

이 여섯 개는 **이름 그대로 두고 값도 바꾸지 않습니다.** 하나를 바꾸면 블로그와 웹앱 전부가 어긋납니다.

## 2. 역할 이름 — 여섯 색을 무엇에 쓰는가

원색을 직접 쓰지 말고 역할 이름을 거쳐 씁니다. 다크 모드에서 갈아끼울 수 있는 층이 이쪽입니다.

```css
--c-bg:            var(--winter-haze);        /* 페이지 바탕 */
--c-surface:       var(--ice-sheet);          /* 카드·보드 등 한 단 위의 면 */
--c-text:          var(--mountain-ink);       /* 본문 */
--c-muted:         var(--stone);              /* 보조 텍스트, 캡션 */
--c-hairline:      rgba(28, 35, 37, 0.14);    /* 경계선 */
--c-hairline-soft: rgba(28, 35, 37, 0.07);    /* 더 약한 경계선 */
--c-accent:        var(--moss);               /* 링크·강조 — 유일한 색 */
--c-accent-hover:  #354a46;
--c-code-bg:       rgba(255, 255, 255, 0.5);
```

## 3. 유리 — 일곱 번째 재료

안개 낀 아침 창처럼, 뒤가 비치되 흐리게. 헤더·다이얼로그·떠 있는 면에 씁니다.

```css
--glass-bg:        rgba(255, 255, 255, 0.5);
--glass-bg-strong: rgba(255, 255, 255, 0.68);
--glass-border:    rgba(255, 255, 255, 0.65);
--glass-blur:      blur(22px) saturate(150%);
--glass-shadow:    0 1px 1px rgba(28, 35, 37, 0.03),
                   0 8px 28px rgba(28, 35, 37, 0.06);
```

```css
.panel {
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  -webkit-backdrop-filter: var(--glass-blur);
  backdrop-filter: var(--glass-blur);
}
```

유리를 쓸 때 지킬 것.

**유리는 뒤에 무언가 있을 때만 유리입니다.** 단색 바탕 위에 얹으면 그냥 반투명한 회색 사각형입니다.

**유리 위에 유리를 얹지 않습니다.** 밝은 반투명 면을 겹치면 가독성이 무너집니다. 겹쳐야 한다면 아래 층을 불투명하게 바꿉니다.

**큰 면일수록 두껍게 읽혀야 합니다.** 넓은 면에는 더 강한 블러와 더 깊은 그림자를, 작은 칩에는 약하게. `--glass-bg-strong`이 그 용도입니다.

**경계선 대신 스크롤 엣지.** 떠 있는 헤더 밑에 1px 실선을 긋지 않습니다. 내용이 유리 밑으로 들어갈 때만 경계가 생기게 합니다. 블로그는 `theme.js`가 `scrollY > 4`에서 `.is-overlapping`을 붙이고, CSS가 그때만 그림자를 켭니다.

**가리려면 흐리고, 흐름을 끊지 않으려면 그대로 둡니다.** 모달처럼 다른 일을 막는 것은 뒤를 어둡게 덮습니다. 나란히 열리는 패널은 덮지 않고 유리와 오프셋만으로 구분합니다.

## 4. 다크 모드 — 겨울밤의 산

역할 이름만 갈아끼웁니다. 바탕색 여섯은 건드리지 않습니다.

```css
--c-bg:            #12181a;
--c-surface:       #1a2224;
--c-text:          #e6ecec;
--c-muted:         #8a9296;                      /* 라이트와 동일 */
--c-hairline:      rgba(230, 236, 236, 0.14);
--c-hairline-soft: rgba(230, 236, 236, 0.07);
--c-accent:        #7ea19a;                      /* 어두운 바탕에서 이끼색을 올린 값 */
--c-accent-hover:  #9ab9b2;
--c-code-bg:       rgba(255, 255, 255, 0.055);

--glass-bg:        rgba(255, 255, 255, 0.055);
--glass-bg-strong: rgba(255, 255, 255, 0.09);
--glass-border:    rgba(255, 255, 255, 0.12);
--glass-shadow:    0 1px 1px rgba(0, 0, 0, 0.2),
                   0 8px 28px rgba(0, 0, 0, 0.28);
```

다크에서 `--c-accent`가 `#4b6560`이 아니라 `#7ea19a`인 이유는, 어두운 바탕 위에서 원래 이끼색은 명도 대비가 부족해 링크로 읽히지 않기 때문입니다.

### 세 가지 상태를 모두 처리한다

테마는 두 가지가 아니라 세 가지입니다 — 명시적 라이트, 명시적 다크, 그리고 **아무것도 고르지 않은 기본값**. 기본값일 때는 `data-theme` 속성이 아예 없으므로 시스템 설정만이 단서입니다.

```css
/* 1. 라이트를 기본으로 정의 */
:root { --c-bg: #eef1f2; /* … */ }

/* 2. 시스템이 다크일 때 */
@media (prefers-color-scheme: dark) {
  :root { --c-bg: #12181a; /* … */ }
}

/* 3. 수동 토글은 시스템 설정을 이긴다 (속성 선택자라 명시도가 높음) */
:root[data-theme="dark"]  { --c-bg: #12181a; /* … */ }
:root[data-theme="light"] { --c-bg: #eef1f2; /* … */ }
```

셋 중 하나라도 빠지면 시스템 다크 사용자가 흰 화면을 보거나, 토글이 한쪽 방향으로만 먹습니다.

## 5. 타이포그래피

```css
--font-sans: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont,
             "Apple SD Gothic Neo", "Segoe UI", Roboto, "Malgun Gothic", sans-serif;
--font-mono: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, monospace;
```

### 트래킹은 크기마다 다르다

**하나의 `letter-spacing` 값을 전체에 쓰면 어딘가에서는 반드시 틀립니다.** 글자는 커질수록 사이가 벌어져 보이고, 작아질수록 붙어 보입니다. 그래서 큰 글자는 조여 주고 작은 글자는 벌려 줍니다.

블로그가 실제로 쓰는 값입니다.

| 쓰임 | letter-spacing |
| --- | --- |
| 큰 제목 | `-0.035em` ~ `-0.025em` |
| 중간 제목 | `-0.02em` ~ `-0.015em` |
| 본문 | `-0.01em` ~ `0` |
| 작은 라벨 (대문자) | `0.06em` ~ `0.22em` |

`Studies in Sensibility`나 `Essays` 같은 섹션 라벨이 `0.22em`인 것은, 작은 대문자는 벌려야 읽히기 때문입니다.

### 행간은 크기에 반비례한다

큰 제목은 좁게(`1.05` 근처), 본문은 넉넉하게(`1.5` 이상). 제목의 행간이 본문만큼 벌어지면 두 줄이 한 덩어리로 읽히지 않습니다.

### 위계는 크기만으로 만들지 않는다

굵기·크기·행간을 한 세트로 씁니다. 굵기는 자리를 더 차지하지 않으면서 존재감을 주므로, 크기를 키우기 전에 굵기를 먼저 고려합니다.

간격은 `rem`/`em`으로 씁니다. 사용자가 글자 크기를 키웠을 때 레이아웃이 함께 커져야 합니다.

## 6. 간격 · 반경 · 폭

```css
--w-text:   42rem;   /* 본문 한 줄 최대 폭 */
--w-wide:   62rem;   /* 넓은 레이아웃 */

--radius:    14px;
--radius-sm:  8px;

--space-1: 0.5rem;
--space-2: 1rem;
--space-3: 1.5rem;
--space-4: 2.5rem;
--space-5: 4rem;
--space-6: 6.5rem;
```

간격은 이 여섯 단계 안에서 고릅니다. `1.3rem` 같은 값이 필요해 보이면 대개 레이아웃이 잘못된 것입니다.

## 7. 모션

근거는 `apple-design` 스킬입니다. 여기에는 이 저장소에서 실제로 지킬 것만 옮겨 적습니다.

### 반응은 누르는 순간에

**`click`이 아니라 `pointerdown`에서 반응합니다.** 손을 떼기를 기다리면 죽은 것처럼 느껴집니다. 블로그는 `theme.js`가 포스트 링크·앱 카드·테마 토글·사이트 제목에 `pointerdown`으로 `.is-pressed`를 붙이고, `pointerup`/`pointercancel`/`pointerleave`에서 뗍니다.

```css
.button:active,
.button.is-pressed {
  transform: scale(0.97);
  transition: transform 0.1s ease-out;
}
```

축소는 `0.97` 근처, 시간은 `0.1s`. 이보다 크게 줄이면 장난스러워지고, 느리면 반응이 아니라 애니메이션이 됩니다.

### 제스처가 없으면 transition, 있으면 스프링

이 저장소의 대부분은 제스처가 없습니다. 색·경계선·눌림 같은 것은 CSS `transition`으로 충분하고, 블로그의 표준 값은 **`0.2s ease`**(색·경계선)와 **`0.1s ease-out`**(눌림)입니다. 새로 만들 때 이 두 값을 벗어날 이유가 있는지 먼저 따져 보세요.

제스처가 걸린 요소는 다릅니다. **드래그·스와이프·시트에는 `transition`과 `@keyframes`를 쓰지 않습니다.** 정해진 시간 동안 정해진 경로를 재생할 뿐이라 도중에 붙잡아 되돌릴 수 없습니다. 이런 요소는 JS 스프링이 움직입니다.

### 스프링 값

| 상황 | damping | response |
| --- | --- | --- |
| 기본 UI (이동·정렬) | `1.0` | `0.3` ~ `0.4` |
| 던진 뒤 (플릭·드래그 릴리스) | `0.8` | `0.3` ~ `0.4` |

**기본은 `damping 1.0`, 즉 튕김 없음입니다.** 튕김은 제스처가 실제로 운동량을 실었을 때만 씁니다. 그냥 나타난 메뉴가 통통 튀면 어색하고, 손가락으로 던진 카드가 튀면 자연스럽습니다.

이 저장소의 절제된 결에서는 튕김을 쓸 일이 거의 없습니다. 쓰기 전에 "사용자가 이걸 던졌나?"를 자문하세요.

### 붙잡을 수 있어야 한다

움직이는 중에 다시 붙잡으면 **그 자리에서** 손가락을 따라와야 합니다. 닫히는 시트를 다시 잡았을 때 끝까지 닫힌 뒤 다시 열리면 안 됩니다.

구현의 핵심은 하나입니다. **새 애니메이션은 목표값이 아니라 화면에 지금 그려져 있는 값에서 시작합니다.** 논리적 목표값에서 시작하면 눈에 보이는 점프가 생깁니다.

### 놓은 속도를 이어받는다

드래그가 끝나면 애니메이션이 손가락의 속도 그대로 이어져야 합니다. 여기서 이음매가 보이면 "괜찮다"와 "매끄럽다"가 갈립니다.

그리고 **놓은 지점이 아니라 갈 곳으로 보냅니다.** 속도로 도달점을 예측한 뒤, 그 지점에서 가장 가까운 스냅 위치를 목표로 삼습니다.

```js
// 감속률 0.998 = 일반적인 스크롤 감각, 0.99 = 더 빠릿함
function project(velocity /* px/s */, deceleration = 0.998) {
  return (velocity / 1000) * deceleration / (1 - deceleration);
}
const landing = current + project(releaseVelocity);
```

### 들어온 길로 나간다

오른쪽에서 들어온 패널은 오른쪽으로 나갑니다. 들어올 때와 나갈 때의 경로가 다르면 공간 감각이 깨집니다. 그리고 메뉴·팝오버는 **자기를 연 버튼에서** 자라나야 합니다 — `transform-origin`을 트리거 쪽에 둡니다.

### 경계에서 고무줄

끝에 닿았을 때 딱 멈추지 않고 점점 저항합니다. 딱 멈추면 "고장났다"로 읽히고, 저항하면 "반응하고 있지만 더는 없다"로 읽힙니다.

## 8. 접근성

세 가지 신호를 각각 처리합니다. 블로그의 `public/styles.css`는 셋 다 대응하고 있으니 그 구조를 그대로 가져가면 됩니다.

```css
/* 움직임을 줄여 달라 — 없애는 게 아니라 부드럽게 바꾼다 */
@media (prefers-reduced-motion: reduce) {
  .sheet { transition: opacity 0.2s ease; transform: none !important; }
}

/* 투명을 줄여 달라 — 유리를 불투명하게 */
@media (prefers-reduced-transparency: reduce) {
  .toolbar { background: var(--c-bg); backdrop-filter: none; }
}

/* 대비를 높여 달라 — 경계선을 뚜렷하게 */
@media (prefers-contrast: more) {
  .card { border: 1px solid var(--c-text); }
}
```

`prefers-reduced-motion`은 **피드백을 없애라는 뜻이 아닙니다.** 미끄러짐·스프링·패럴랙스를 짧은 페이드로 바꾸고 튕김을 뺍니다. 이해를 돕는 색·투명도 변화는 남깁니다.

그 밖에: 화면 전체를 덮는 움직이는 배경, 5초에 한 번꼴로 느리게 반복하는 진동, 갑작스러운 밝기 점프를 피합니다. 다크/라이트 전환도 부드럽게 넘깁니다.

터치 타깃은 최소 44×44px입니다.

## 9. 웹앱에서 쓰는 법

웹앱은 `/apps/{앱 이름}/` 안에서 자체 완결합니다. 블로그의 `public/styles.css`를 링크하지 않습니다 — 앱은 독립적으로 열려야 하고, 블로그 CSS에는 앱에 필요 없는 규칙이 대부분입니다.

그래서 **값을 복사해 옵니다.** 앱의 `style.css` 맨 위에 위 토큰 중 그 앱이 실제로 쓰는 것만 옮겨 적고, 나머지는 앱 고유의 이름으로 정의합니다. 기존 앱 세 개가 모두 이 방식입니다.

만들기 전에 확인할 것.

- 여섯 바탕색의 hex 값을 바꾸지 않았는가. 이름은 앱 사정에 맞게 바꿔도 되지만 값은 그대로입니다.
- 이끼색을 누를 수 있는 것에만 썼는가.
- 다크 모드 세 상태(4절)를 모두 만들었는가.
- `pointerdown`에서 반응하는가. `click`을 기다리고 있지 않은가.
- 제스처가 있다면 스프링으로 움직이는가. `transition`으로 드래그를 흉내내고 있지 않은가.
- `prefers-reduced-motion`·`prefers-reduced-transparency`·`prefers-contrast` 셋을 모두 대응했는가.
- 터치 타깃이 44×44px 이상이고, 가로 스크롤이 생기지 않는가.
- 사용법 안내 문구가 있는가. `CLAUDE.md`의 웹앱 규칙입니다.

## 10. 지금 어긋나 있는 것

이 문서를 쓰면서 실제 코드를 훑어 확인한 것들입니다.

**`calculator` 앱에 접근성 대응이 없습니다.** `prefers-reduced-motion`·`prefers-reduced-transparency`·`prefers-contrast` 셋 다 없습니다. `2048`과 `pixel-art`는 `prefers-reduced-motion`만 있고 나머지 둘은 없습니다. 블로그 본체는 셋 다 대응하고 있어서, 앱만 뒤처진 상태입니다.

**제스처가 있는 앱이 스프링을 쓰지 않습니다.** `2048`과 `pixel-art`가 포인터 이벤트를 다루지만 움직임은 CSS `transition`입니다. 두 앱 모두 드래그 도중에 방향을 바꾸는 상호작용은 아니라서 당장 문제는 아니지만, 7절의 기준으로는 미달입니다.

**팔레트가 다섯 곳에 복사되어 있습니다.** `public/styles.css`, 앱 세 개의 `style.css`, 그리고 이 문서. 앱이 독립적으로 열려야 한다는 제약과 맞바꾼 결과라 당장 고칠 문제는 아니지만, 색을 바꿀 일이 생기면 다섯 곳을 모두 훑어야 합니다.
